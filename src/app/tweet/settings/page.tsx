import Update from "@/component/settings/settings";

export default function Settings() {
  return (
    <div className="h-full w-full p-10 overflow-y-auto flex justify-center scrollbar-hide relative">
      <Update/>
    </div>
  );
}
